# 单包仓库：仅借 pnpm-workspace.yaml 承载 pnpm 11 的构建脚本批准配置。
# packages 字段必填（否则 `pnpm store path` 等命令报 "packages field missing or empty"）。
packages:
  - .
# pnpm 11 默认不运行依赖的构建脚本，需在此显式批准。
# protobufjs 的 postinstall 在无 versionScheme 时为空操作，批准其构建以消除 ERR_PNPM_IGNORED_BUILDS。
allowBuilds:
  protobufjs: true
